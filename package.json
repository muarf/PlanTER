{
  "name": "ter-finder",
  "version": "0.1.0",
  "private": true,
  "description": "planTER — moteur de recherche 100% TER. SPA web + shell natif Capacitor.",
  "scripts": {
    "sync": "cap sync android",
    "build": "cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
